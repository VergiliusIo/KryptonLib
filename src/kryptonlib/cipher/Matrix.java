package kryptonlib.cipher;

import kryptonlib.abstraction.SymmetricCipher;

/**
 * Created by USER1 on 09.05.2017.
 */
public class Matrix extends SymmetricCipher {
    /*public String encrypt(String plaintext, String keyM, String keyN) {
        int m = keyM.length();
        int n = keyN.length();

        kryptonlib.Matrix matrix = new kryptonlib.Matrix(m + 1, n);

        for (int i = 1; i < n + 1; i++) {
            char letter = keyN.charAt(i - 1);
            //matrix.matrix[0][i] = letter;
            String a = new String("");

        }
    }*/

}
